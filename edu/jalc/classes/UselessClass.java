package edu.jalc.inclass;
class UselessClass{}//dont need public for [default]
//public makes it accesible to everyone. It is a visibility modifier 4)

/*private - class itself can only access it. most restrictive 1)

  protected- package and subclasses can access it, class itself 3)
             
  package.private[default]- only the package can access it, class itself 2)
  
  Why do we have that?*/

